# Example environment variables for integration tests
# Copy this file to tests/.env and fill in the values from your Terraform outputs

# DynamoDB table name for event deduplication
# Get with: cd terraform && terraform output -raw dynamodb_table_name
DYNAMODB_TABLE_NAME=hello-agent-slack-events

# Step Functions state machine ARN
# Get with: cd terraform && terraform output -raw state_machine_arn
STATE_MACHINE_ARN=arn:aws:states:ap-northeast-1:123456789012:stateMachine:hello-agent-event-processor

# Lambda function name (receiver)
# Get with: cd terraform && terraform output -raw lambda_function_name
LAMBDA_FUNCTION_NAME=hello-agent

# AWS region (optional, default: ap-northeast-1)
AWS_REGION=ap-northeast-1
